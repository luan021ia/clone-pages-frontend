describe('Teste Simples', () => {
  it('deve passar', () => {
    expect(1 + 1).toBe(2);
  });

  it('deve testar booleanos', () => {
    expect(true).toBe(true);
    expect(false).toBe(false);
  });
});